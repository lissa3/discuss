# from django_filters import FilterSet, CharFilter, NumberFilter

# from ideas.models import Idea

# class IdeaFilter(FilterSet):
#     title = CharFilter(name='title', lookup_type='icontains', distinct=True)
#     categ = CharFilter(name='categs_name', lookup_type='icontains', distinct=True)
#     categ_id = CharFilter(name='categ_id',lookup_type='icontains',distinct=True)
#     author = CharFilter(name='author__profile_display_name', lookup_type='icontains', distinct=True)	
#     author_id = CharFilter(name='author_id', lookup_type='icontains', distinct=True)
#     class Meta:
#         model = Idea
#         fields = ['categ','title','categ_id','author','author_id']

	
	
		